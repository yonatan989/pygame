import random
import pygame
import sys
sys.path.append('/home/yonatan/PycharmProjects/PyIgnition')
print(sys.path)
import PyIgnition
from collections import Iterable
'''
try:
    from PyIgnition import *
except ImportError:
    from .PyIgnition import *

# Constants
WINDOW_WIDTH = 1920
WINDOW_HEIGHT = 1080

# Images path's'
IMAGE = r'/home/yonatan/Documents/Python/pictures/madagascar_1.jpg'

# Initialication
pygame.init()
size = (WINDOW_WIDTH, WINDOW_HEIGHT)
position = (100,100)
screen = pygame.display.set_mode(size)
pygame.display.set_caption("Game")
effect = PyIgnition.ParticleEffect(screen,position, size)


img = pygame.image.load(IMAGE)
# Arguments:
#   #display: the surface to draw to
#   #position: the position of the effect on-screen
#   #size: the size of the effect (normallyjust the display size)
'''